# 🛒 BlUE BUTCHER Microservices Backend

![Java](https://img.shields.io/badge/Java-Spring%20Boot-green)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED)
![Kafka](https://img.shields.io/badge/Apache-Kafka-black)
![Redis](https://img.shields.io/badge/Redis-Cache-red)
![Elasticsearch](https://img.shields.io/badge/Elasticsearch-Search-yellow)

Масштабируемая, отказоустойчивая микросервисная платформа для интернет-магазина. Проект спроектирован с упором на производительность операций чтения, целостность данных и удобство развертывания в контейнеризированной среде.

---

## 🏗 Архитектура и База Данных

В основе системы лежит **PostgreSQL 16**. Схема данных спроектирована с использованием UUID для первичных ключей, что обеспечивает глобальную уникальность и упрощает шардирование.

### Особенности проектирования БД:
*   **Нормализация и целостность:** 9 основных таблиц (`users`, `products`, `comments`, `rating` и др.) связаны строгими внешними ключами с каскадным удалением (`ON DELETE CASCADE`).
*   **Оптимизация (Денормализация):**
    *   Дублирование `username` в таблице комментариев для исключения лишних JOIN'ов.
    *   Агрегированные поля в `products` (рейтинг, количество продаж/комментариев), обновляемые асинхронно.
*   **Безопасность:** Изолированные доступы и управление сессиями через refresh-токены.

---

## 🧩 Микросервисы

Система разделена на независимые компоненты, работающие в единой сети Docker:

| Сервис | Описание | Технологии |
| :--- | :--- | :--- |
| **UserService** | Управление профилями, авторизация, безопасность. | Spring Security, JWT, Kafka |
| **ProductService** | Каталог товаров, цены, остатки. Кэширование популярных товаров. | Redis, Kafka |
| **BuyService** | Обработка транзакций покупок и истории заказов. | Kafka |
| **CommentsService** | Управление отзывами и комментариями пользователей. | Kafka |
| **SearchService** | Полнотекстовый поиск по каталогу, синхронизация с БД. | Elasticsearch, Kafka |
| **Frontend** | Пользовательский интерфейс приложения. | Angular |

---

## 🚀 Инфраструктура и Инструменты

Проект реализует гибридную архитектуру хранения и обработки данных:

*   🐘 **PostgreSQL 16:** Основное хранилище данных (Source of Truth).
*   🚀 **Apache Kafka:** Асинхронный обмен событиями (обновление счетчиков, индексация поиска) для разгрузки основного потока.
*   ⚡ **Redis:** Кэширование часто запрашиваемых данных.
*   🔍 **Elasticsearch:** Быстрый полнотекстовый поиск.
*   🔄 **Nginx:** Reverse proxy и балансировка запросов.
*   💾 **Auto-Backups:** Отдельный сервис для создания дампов БД и отправки их в приватный репозиторий GitHub.

---

## 🛠 Установка и Запуск

Проект полностью докеризирован. Для запуска требуется **Docker** и **Docker Compose**.

### 1. Клонирование репозитория
```bash
git clone https://github.com/CherteelA/blue-butcher-shop.git
cd blue-butcher-shop
```

### 2. Настройка окружения
Создайте файл .env в корне проекта и заполните необходимые переменные, все переменные представлены в .env.example

### 3. Генерация RSA ключей безопасности
Система использует JWT (RS256) для авторизации. Так как приватные ключи не хранятся в репозитории, их необходимо сгенерировать и разложить по сервисам перед запуском.

Для удобства вы можете использовать OpenSSL. Выполните следующие команды в корне проекта (Git Bash / Terminal):

#### 1. Генерация пары ключей
```bash
# Создать приватный ключ
openssl genrsa -out private.pem 2048

# Создать публичный ключ из приватного
openssl rsa -in private.pem -pubout -out public.pem
```
#### 2. Распределение ключей по сервисам
Добавить public.pem в папку resources каждого микросервиса,
а также private.pem в микровервисы SearchService и UserService

### 4. Запуск контейнеров
```bash
docker-compose up -d --build
```
**Автор: Ситников Алексей**
